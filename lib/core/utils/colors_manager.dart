import 'package:flutter/material.dart';

abstract class ColorsManager {
  static const primary = Color(0xff100B20);
  static const secondary = Colors.white;
  
  static const black = Colors.black;
  static const pink = Color(0xffEF8262);

  static const lightGrey = Color.fromRGBO(255, 255, 255, 0.7);
  static const darkGrey = Color.fromRGBO(255, 255, 255, 0.5);

  static const starColor = Color(0xffFFDD4F);
}
